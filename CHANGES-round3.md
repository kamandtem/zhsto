# دور سوم تغییرات

- ترتیب صفحه ژست اصلاح شد: مراحل اجرا، چی بگم به سوژه، تنوع، فیلم‌برداری همین ژست، چک‌لیست اجرای سریع، سپس جزئیات.
- عنوان «تنوع‌های همین ژست» به «تنوع» تغییر کرد.
- در افزودن/ویرایش ژست، فیلد تنوع اضافه شد و داخل `Pose.variations` ذخیره می‌شود.
- کنار دکمه «بعدی» یک میانبر آیکنی دوربین فیلم‌برداری اضافه شد.
- هنگام ورود به ویرایش بخش‌های پلان فیلم، متن پیش‌فرض برای پلان جدید خودکار پاک می‌شود؛ داده‌های ذخیره‌شده قبلی دست‌نخورده می‌مانند.
