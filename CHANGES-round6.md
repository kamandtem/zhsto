# دور ششم، بررسی کلی و بهینه‌سازی

## بررسی و اصلاح
- مسیرهای قدیمی حالت عکاسی و ورودی‌های باقی‌مانده بررسی و حذف شدند.
- رفرنس‌های کامپوننت‌های حذف‌شده بررسی شدند.
- اتصال هدر، نوار پایین، منو، دفتر آتلیه و پروفایل بررسی شد.
- اتصال صفحه خانه، کتابخانه، علاقه‌مندی‌ها، جزئیات ژست و اصول ژست‌دهی بررسی شد.
- زوم عکس در ادیتور با حفظ نسبت واقعی تصویر بررسی شد.

## سرعت و روانی
- تأخیر مصنوعی اسپلش از ۱.۸۵ ثانیه به حدود ۱.۰۵ ثانیه کاهش یافت، بدون حذف انیمیشن ورود.
- `PoseVisual` و `PoseCard` memo شدند تا تغییرات نامرتبط، همه‌ی کارت‌های تصویری را دوباره رندر نکند.
- بارگذاری تصاویر همچنان lazy است و Leaflet به‌صورت dynamic import باقی مانده تا نقشه روی شروع برنامه وارد bundle اصلی نشود.
- فایل‌های تصویری سنگین و مسیرهای بارگذاری اولیه بررسی شدند.

## اسپلش
- در اسپلش وب و فایل SVG اسپلش native، متن «برنامه‌نویس: محمدرضا ارجمند» اضافه شد.
