# اجرای بخش ۶ و ۷.۱ از راهنمای بازطراحی (بخش ۷.۲ عمداً انجام نشد)

## بخش ۶ — ترتیب اجرا
مراحل ۱، ۲، ۳، ۵ و ۶ (تایپ‌ها، `locations.ts`، `Filters.tsx`/`PoseCard.tsx`، رفع GIF در
`media.ts`، و `AddPoseSheet.tsx`) از قبل در کد کامل بودند؛ فقط مرحله ۴ باقی مانده بود:

- به `Seed` در `src/data/poses.ts` فیلد اختیاری `gsc?: GardenSubCategory` اضافه شد و
  `build()` آن را فقط وقتی `loc` شامل «باغ عمارت» باشد به `gardenSubCategory` ژست نهایی
  وصل می‌کند.
- به هر ۷۶ ژستی که یکی از لوکیشن‌هایش «باغ عمارت» بود (در `poses.ts`، شامل
  `SEEDS_A/B/C/D` و `GARDEN_SEEDS`)، بر اساس عنوان/تگ/نوع ژست یک `gsc` منطقی نسبت داده
  شد (جزئیات و دکور / آماده شدن عروس / آماده شدن داماد / پرتره زوج / خانواده و گروهی /
  رقص و شادی / شب و بدرقه). طبق بخش ۳ راهنما، این حدس‌هاست و باید توسط شما تأیید/اصلاح
  شود — لیست کامل عنوان → دسته در پیام چت آورده شده.
- **عمداً بدون `gsc` رها شدند:** ۳۰ ژست در `featureSeeds.ts` (دوچرخه/برف/باران/ماشین/
  مزرعه) که به‌صورت پیش‌فرض `loc` شامل «باغ عمارت» می‌شوند ولی موضوعشان مختص باغ نیست؛
  فیلد اختیاری است، پس بدون مشکل زیر فیلتر «همه مراحل» نمایش داده می‌شوند.
- مورد ۷ بخش ۶ (اسکریپت import بسته‌های زیپ) اجرا نشد چون خود راهنما آن را «اختیاری،
  آخرین اولویت» علامت زده بود؛ ضمناً `scripts/import-pose-pack.mjs` از قبل در پروژه
  وجود دارد.

## بخش ۷.۱ — شروع سریع بر اساس موقعیت الان
- **`src/components/QuickStartSheet.tsx` (جدید):** کادر انتخاب لوکیشن؛ اگر «باغ عمارت»
  انتخاب شود، مرحله دوم برای انتخاب زیردسته یا «همه مراحل، پشت سر هم» باز می‌شود.
- **`src/components/ShootMode.tsx`:** پراپ اختیاری `queuePosition` اضافه شد؛ وقتی صف از
  شروع سریع می‌آید، به‌جای دکمه بی‌معنای «قفل دسته‌بندی»، شماره جایگاه در صف (مثلاً
  «۳ / ۱۲») نشان داده می‌شود و روی آخرین آیتم دکمه «ژست بعدی» به «پایان صف» تغییر می‌کند.
- **`src/App.tsx`:** دکمه در `HomeView` کادر `QuickStartSheet` را باز می‌کند؛
  `startQuickShoot(location, gsc)` صف را با `filterPoses` + `sortForProgression` (مرتب
  بر اساس `ease`: یخ‌شکن → صمیمی/حرفه‌ای) می‌سازد و `ShootMode` را باز می‌کند؛
  `quickShootNext` در صف جلو می‌رود و در پایان با toast صف را می‌بندد. دکمه برگشت
  سخت‌افزاری/مرورگر هم این دو کادر را قبل از بقیه ناوبری می‌بندد.
- **`src/views/HomeView.tsx`:** دکمه اصلی «الان کجای مراسمی؟ — شروع سریع» زیر متن هیرو
  اضافه شد (پراپ جدید `onQuickStart`).

## بخش ۷.۲ — انجام نشد (طبق درخواست شما)
فیلد `gestureCue` به تایپ `Pose` اضافه نشد.

## نکته برای ادامه کار
تایپ‌چک کامل (`npm run typecheck`) روی این محیط اجرا نشد چون `node_modules` نصب نیست و
شبکه در دسترس نبود؛ ادیت‌ها با بررسی دستی و بالانس براکت/آکولاد تأیید شدند، ولی پیشنهاد
می‌شود قبل از build نهایی یک‌بار `npm install && npm run typecheck` را خودتان اجرا کنید.
