// Add to pose.ts after ProjectInvoice interface

export interface InvoiceRecord {
  id: string;
  title: string;
  customerName: string;
  date: string;           /** ISO date */
  items: Array<{ name: string; count: number; price: number }>;
  total: number;
  createdAt: number;
  updatedAt: number;
}
