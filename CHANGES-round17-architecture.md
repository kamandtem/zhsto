# راند ۱۷ — بازنگری معماری محتوایی: از «Location-based Pose Library» به «Photography Scenario + Universal Pose Library»

این راند یک مرتب‌سازی دسته‌بندی نیست؛ تغییر مدل ذهنی محصول است.

---

## ۱. معماری فعلی (پیش از تغییر) — آنچه در کد بود

| لایه | وضعیت قبلی |
|---|---|
| `LocationType` | ۶ لوکیشن: شمال، جنوب، ساحل، کویر، شهر، باغ عمارت |
| `GardenSubCategory` | ۸ زیردسته، **فقط برای باغ عمارت** معنا داشت |
| `CategoryType` | دسته سوژه: عروس و داماد / عروس / داماد / زوج / گروهی |
| `Pose.locations` | آرایه لوکیشن = عملاً «مالکیت» ژست |
| `FilterState` | فیلتر اصلی = `location`، و `gardenSubCategory` **مشروط به** `location === 'باغ عمارت'` |
| ناوبری | تب «لوکیشن» در نوار پایین، بخش «بر اساس لوکیشن» در بالای خانه |
| شروع سریع | قدم اول = انتخاب لوکیشن |

### توزیع واقعی داده (۱۲۳ Seed + مرجع + Feature)

```
باغ عمارت 76   کویر 47   شمال 45   جنوب 43
ساحل 0    شهر 0        ← دو لوکیشن کامل خالی بودند
یک‌لوکیشنی: 62 ژست     ← نیمی از کتابخانه به یک محیط چسبیده بود
gardenSubCategory: 47 ژست «هیچ»، و 40 ژست همه در «پرتره زوج»
```

### کدام بخش‌ها بیش از حد Location-based بودند

1. **`Pose.locations` به‌عنوان مالکیت.** `filterPoses` می‌گفت `p.locations.includes(f.location)`. یعنی ژست «آغوش از پشت» اگر عکسش در باغ گرفته شده بود، در کویر وجود نداشت.
2. **`GardenSubCategory` تنها جای درست محصول بود، اما در جای غلط.** توالی واقعی کار تصویربرداری (جزئیات → آماده شدن → پرتره → …) به‌عنوان **زیرمجموعه یک لوکیشن** تعریف شده بود، نه محور محصول.
3. **`Filters`** لوکیشن را اولین ردیف نشان می‌داد و «مرحله» فقط با انتخاب باغ عمارت ظاهر می‌شد.
4. **`QuickStartSheet`** اول لوکیشن می‌پرسید و برای غیرِ باغ عمارت هیچ مرحله‌ای نداشت.
5. **`HomeView`** «بر اباس لوکیشن» را بالاتر از هر چیز دیگری می‌گذاشت.
6. **`PoseCard`** فقط زیردسته باغ عمارت را برچسب می‌زد؛ بقیه ژست‌ها بی‌هویت بودند.
7. **`ساحل` و `شهر` صفر ژست داشتند** — نشانه قطعی اینکه مدل شکسته است: ژست‌های عمومی به آن دو محیط «تخصیص» داده نشده بودند، در حالی که همه‌شان آن‌جا اجرا می‌شوند.

---

## ۲. Information Architecture جدید

```
هسته محصول  =  Photography / Videography Scenario
│
├── ۱  جزئیات و اکسسوری        Details & Accessories
│      └── دکور · حلقه · دسته‌گل · لباس · کفش · اکسسوری
├── ۲  آماده شدن عروس           Bride Getting Ready
├── ۳  آماده شدن داماد          Groom Getting Ready
├── ۴  نگاه اول و بهم رسیدن     First Look / Meeting
├── ۵  پرتره عروس               Bride Portraits      (کادر: کلوز/مدیوم/واید)
├── ۶  پرتره داماد              Groom Portraits      (کادر: کلوز/مدیوم/واید)
├── ۷  پرتره زوج                Couple Portraits
├── ۸  تعامل زوج                Couple Interaction
├── ۹  قدم زدن و حرکت           Walking & Movement
├── ۱۰ شادی، احساس و رقص        Fun / Emotion / Dance
├── ۱۱ خانواده و گروهی          Family & Group
├── ۱۲ مراسم و ورود             Ceremony / Entrance
└── ۱۳ شب و بدرقه               Night / Farewell

Location  =  Context (لایه دوم، نه محور)
├── راهنمای اجرایی محیط: نور، لباس، لنز، بهترین ساعت، خطاها
├── سبد A: ژست‌های عمومی سازگار با این محیط   (محاسبه‌شده)
└── سبد B: ژست‌های اختصاصی همین محیط          (وابستگی واقعی)
```

**نقش دقیق هر لوکیشن**

| مفهوم | نقش |
|---|---|
| **General** | هسته کتابخانه. مستقل از لوکیشن، در اکثر محیط‌ها اجرا می‌شود. پیش‌فرض هر ژست. |
| **Garden (باغ عمارت)** | یک Location Context. مرجع طراحی اولیه محتوا بود (کامل‌ترین سناریوی Pre-Wedding)، اما **محور نیست**. هر چیزی که وابستگی واقعی به معماری عمارت ندارد، General است. |
| **North / South / Beach / Desert / Urban** | Location Context. فقط ژست‌هایی را «مالکِ» خود دارند که به ویژگی فیزیکی همان محیط وابسته‌اند (مه و بافت جنگل، نخلستان و قایق، رمل و خط تپه، نئون و خط عابر). |

---

## ۳. Content Taxonomy جدید — Category در برابر Metadata

| فیلد | نقش | چرا |
|---|---|---|
| `scenario` | **Category اصلی** (تنها یکی) | مرحله واقعی روز تصویربرداری؛ سؤالی که عکاس سر صحنه از خودش می‌پرسد. |
| `detailSubject` | زیر-Category مشروط | فقط برای «جزئیات و اکسسوری» معنا دارد. |
| `scope` | Metadata کلیدی | `عمومی` یا `اختصاصی لوکیشن`. |
| `locationLock` | Metadata | فقط وقتی `scope = اختصاصی`؛ به کدام محیط قفل است. |
| `locationReason` | Metadata متنی | *چرا* وابسته است؛ در صفحه ژست به کاربر توضیح داده می‌شود. |
| `suitableLocations` | Metadata محاسبه‌شده | همه محیط‌هایی که ژست در آن‌ها اجرا می‌شود. **جایگزین `locations` به‌عنوان منطق فیلتر.** |
| `mood` | Tag | رمانتیک / شاد / آرام / دراماتیک / رسمی |
| `framing` | Tag | کلوز / مدیوم / واید |
| `movement` | Tag بولی | سوژه حرکت می‌کند یا نه |
| `environment` | Tag | فضای باز / فضای بسته / هر دو |
| `category` (سوژه) | **از Category به Tag تنزل یافت** | عروس/داماد/زوج/گروهی یک ویژگی است، نه محور ناوبری. |
| `poseType` | Tag | حالت بدن؛ به بخش پیشرفته فیلتر منتقل شد. |
| `locations` | **Legacy** | حفظ شده فقط برای مهاجرت و «تمایل بصری»؛ دیگر مالکیت نیست. |
| `gardenSubCategory` | **Deprecated** | به `scenario` نگاشت می‌شود. |

مثال یک ژست در مدل جدید:

```
Pose id: pose-042  (یک ID یکتا، هیچ‌وقت Duplicate نمی‌شود)
  scenario           : تعامل زوج
  scope              : عمومی
  environment        : هر دو
  mood               : رمانتیک
  movement           : false
  framing            : مدیوم
  suitableLocations  : باغ عمارت، شمال، جنوب، ساحل، کویر، شهر
```

---

## ۴. چطور یک ژست بدون Duplicate در چند Context دیده می‌شود

هیچ رکوردی کپی نمی‌شود. راه‌حل سه تکه دارد:

1. **`deriveScope()`** — موتور تشخیص وابستگی. برای هر لوکیشن یک مجموعه «کلیدواژه سخت» تعریف شده (`قایق`، `رمل`، `مه جنگل`، `ستون عمارت`، `نئون`…). ژست فقط وقتی به یک محیط قفل می‌شود که کلیدواژه سختِ آن را داشته باشد. در غیر این صورت **General** است، حتی اگر عکس نمونه‌اش در یک لوکیشن خاص گرفته شده باشد.
2. **`deriveSuitableLocations()`** — برای ژست General، لیست محیط‌های سازگار **محاسبه** می‌شود (فضای بسته → باغ و شهر؛ فضای باز → همه). خروجی: یک رکورد، حضور در چند Context.
3. **`runsIn(pose, location)`** — فیلتر لوکیشن از `p.locations.includes(loc)` به `suitableLocations.includes(loc)` تغییر کرد. معنای فیلتر از «متعلق به این‌جا» به **«قابل اجرا این‌جا»** عوض شد.

برای تصحیح خطاهای موتور، جدول `SCOPE_OVERRIDES` وجود دارد (کلید = عنوان ژست).

### نتیجه اجرای موتور روی داده واقعی

```
۷۴٪ عمومی  ·  ۲۶٪ اختصاصی لوکیشن
قفل‌شده‌ها:  شمال ۸  ·  ساحل ۹  ·  کویر ۶  ·  باغ عمارت ۹
```

اعتبارسنجی با مثال‌های خودِ بریف:

| ژست | خروجی موتور |
|---|---|
| قدم زدن روی پل چوبی | **عمومی** ✓ |
| قدم زدن روی مسیر جنگلی مه‌گرفته | **اختصاصی شمال** ✓ |
| نشستن روی قایق کنار اسکله | **اختصاصی جنوب** ✓ |
| خطوط رمل و فرم تپه | **اختصاصی کویر** ✓ |
| آغوش از پشت با نگاه عروس | **عمومی** ✓ |
| پرتره داماد با تنظیم دکمه کت | **عمومی** ✓ (قبلاً به خاطر تطبیق «مه» داخل «دکمه» غلط قفل می‌شد) |

و مهم‌تر: **ساحل و شهر که صفر ژست داشتند، حالا کل هسته عمومی را دارند.**

---

## ۵. تغییرات UI/UX (انجام شد)

| فایل | تغییر |
|---|---|
| `data/taxonomy.ts` **(جدید)** | کل تاکسونومی + موتور استنتاج + کمک‌کننده‌های `runsIn` / `splitByScope` / `groupByScenario`. |
| `types/pose.ts` | ۹ فیلد metadata روی `Pose`؛ `FilterState` با `scenario` به‌عنوان فیلتر اول. |
| `components/ScenarioRail.tsx` **(جدید)** | ریل شماره‌دار ۱۳ مرحله = محور ناوبری. |
| `components/PoseAttributes.tsx` **(جدید)** | کارت ویژگی‌ها در صفحه ژست: بنر «عمومی/اختصاصی» + دلیل وابستگی + لیست محیط‌های قابل اجرا. |
| `views/HomeView.tsx` | ترتیب عوض شد: **۱)** «کجای روز تصویربرداری هستی؟» (۱۳ کارت) → **۲)** «الان چه ژستی می‌خواهی؟» (ورود از سمت Mood) → **۳)** «Location Context» با دو عدد (فیروزه‌ای=عمومی، طلایی=اختصاصی). دسته سوژه به «فیلتر کمکی» تنزل یافت. |
| `views/LibraryView.tsx` | وقتی مرحله انتخاب نشده، ژست‌ها **به ترتیب روز تصویربرداری** گروه‌بندی می‌شوند. بالای صفحه ترازوی «هسته عمومی / اختصاصی لوکیشن» با شمارش. |
| `components/Filters.tsx` | ترتیب جدید: مرحله سناریو → نوع ژست (عمومی/اختصاصی) → «قابل اجرا در لوکیشن» → حال‌وهوا → کادر → (پیشرفته: حرکت، فضا، سوژه، حالت بدن، سختی). هر بخش یک خط توضیح مدل ذهنی دارد. |
| `views/LocationsView.tsx` | عنوان «Location Context». هر کارت دو شمارش جدا. دکمه دوم: «فقط N ژست اختصاصی این محیط». جمله صریح: «ژست‌های عمومی متعلق به این لوکیشن نیستند، فقط با آن سازگارند.» |
| `components/PoseCard.tsx` | برچسب «زیردسته باغ عمارت» حذف شد؛ جایش **مرحله سناریو + نشان عمومی(فیروزه‌ای)/اختصاصی(طلایی)** آمد. |
| `views/PoseDetailView.tsx` | چیپ سناریو به‌عنوان برچسب اصلی؛ لیست لوکیشن‌ها از `suitableLocations` خوانده می‌شود؛ کارت `PoseAttributes` اضافه شد. |
| `components/QuickStartSheet.tsx` | **جابه‌جایی قدم‌ها**: قدم ۱ = مرحله سناریو، قدم ۲ = لوکیشن **اختیاری** («فرقی نمی‌کند، ژست‌های عمومی را بده»). |
| `components/AddPoseSheet.tsx` | فرم ساخت ژست حالا مرحله سناریو، عمومی/اختصاصی، حال‌وهوا، کادر و حرکت می‌پرسد — با توضیح اینکه چه وقت «اختصاصی» درست است. |
| `components/BottomNav.tsx` | تب «ژست‌ها» → **«سناریو»** با آیکن کلاپر. |
| `components/Onboarding.tsx` | اسلاید «بر اساس لوکیشن» به دو اسلاید تبدیل شد: «بر اساس سناریوی تصویربرداری» + «لوکیشن یک Context است». |
| `services/storage.ts` | فیلتر لوکیشن → `runsIn`؛ فیلترهای جدید؛ metadata به haystack جستجو اضافه شد (پس «ژست رمانتیک دونفره» و «مناسب کویر» جواب می‌دهد)؛ `enrichPose` روی هر ژست خوانده‌شده تا داده‌های قدیمی کاربر هم مهاجرت کنند. |
| کلیدهای `SectionGuide` | نسخه‌دار شدند (`library-scenario-v2` …) تا کاربران فعلی هم راهنمای مدل جدید را ببینند. |

---

## ۶. سازگاری و مهاجرت

- هیچ ژستی حذف یا کپی نشد؛ همه IDها دست‌نخورده‌اند.
- `gardenSubCategory` باقی مانده اما `@deprecated` است و به `scenario` نگاشت می‌شود.
- ژست‌های ذخیره‌شده کاربر (`localStorage`) هنگام خواندن از `enrichPose` رد می‌شوند، پس بدون مهاجرت دستی metadata می‌گیرند.
- تمام فیلدهای جدید `optional` هستند؛ اگر مقداری نبود محاسبه می‌شود.

## ۷. قدم بعدی پیشنهادی

1. `SCOPE_OVERRIDES` را با یک بازبینی چشمی روی ۳۲ ژست قفل‌شده کالیبره کن (۱۰ دقیقه کار، بالاترین بازده).
2. سناریوهای خالی را پر کن: «مراسم و ورود» و «پرتره داماد» عملاً محتوا ندارند — این دو حالا در UI با شمارش صفر دیده می‌شوند و شکاف محتوا خودش را لو می‌دهد.
3. «کادر» را برای پرتره‌ها جدی بگیر: هدف بریف سه‌گانه کلوز/مدیوم/واید بود؛ فعلاً اکثر ژست‌ها «مدیوم» تشخیص داده می‌شوند چون `cameraTips.framing` عمومی نوشته شده.
