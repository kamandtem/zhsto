# بازطراحی صفحه «فیلم‌برداری این ژست»

با زدن دکمه «فیلم‌برداری این ژست» (در صفحه جزئیات ژست و در حالت عکاسی)، حالا
یک صفحه ساده باز می‌شود:

- عکس ژست بالای صفحه.
- «حرکت دوربین:» — متن قابل ویرایش.
- «حرکت سوژه:» — متن قابل ویرایش.
- «ابزار حرکتی:» — انتخاب یکی از گیمبال / دوربین روی دست / هلی‌شات.

این سه مقدار روی خود رکورد ژست ذخیره می‌شوند (فیلدهای تازه‌ی `cameraMovement`،
`subjectMovement` و `movementTool` در `Pose`) — دقیقاً از همان مسیر ویرایش
ژست‌های موجود (`savePoseEdit`) یا ژست‌های شخصی (`saveCustomPose`). چون
«آماده‌سازی بسته ژست برای انتقال» (در تنظیمات) همین رکوردهای ژست را می‌خواند،
این تغییرات بدون نیاز به تغییر جدا در `buildPosePackZip` یا اسکریپت
`import-pose-pack.mjs` به‌صورت خودکار داخل بسته خروجی هم لحاظ می‌شوند.

فایل‌های تغییرکرده:
- `src/types/pose.ts` — فیلدهای `cameraMovement`, `subjectMovement`,
  `movementTool` و `MOVEMENT_TOOL_OPTIONS` اضافه شد.
- `src/components/FilmPlan.tsx` — کامل بازنویسی شد (قبلاً یک پلان چندبخشی
  جدا از ذخیره‌سازی بسته انتقال بود).
- `src/views/PoseDetailView.tsx`, `src/components/ShootMode.tsx`,
  `src/App.tsx` — اتصال `onSaved`/`onDataChanged` برای بازخوانی ژست بعد از
  ثبت اطلاعات فیلم‌برداری.

نکته: یادداشت‌های قدیمی پلان فیلم (`pd_film_notes_v1` در `storage.ts`) دست‌نخورده
باقی مانده‌اند اما دیگر در هیچ صفحه‌ای استفاده نمی‌شوند.
