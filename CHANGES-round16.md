# بازطراحی صفحه جزئیات ژست (نوار عکس + دکمه‌های اجرا)

## فایل‌های تغییر‌یافته
- `src/views/PoseDetailView.tsx`
- `src/components/PhotoCropModal.tsx` (جدید)
- `src/services/media.ts` (افزودن `readFileAsDataUrl`)

## تغییرات

**نوار زیر عکس (که با تغییر طول متن لوکیشن شکل عوض می‌کرد):**
- ردیف «۱ نفر / لوکیشن» از ردیف دکمه‌ها جدا شد؛ حالا هرکدام روی خط خودش است و طول متن
  لوکیشن دیگر روی اندازه دکمه‌ها اثر نمی‌گذارد (هر دو دکمه `flex-1` با عرض مساوی و ثابت).
- دکمه سطلِ حذف عکس مرجع (تِرش سمت چپ «تغییر عکس») حذف شد.
- دکمه تغییر نسبت عمودی/افقی (آیکن مستطیل) از این نوار حذف شد؛ انتخاب ابعاد حالا هنگام
  آپلود عکس انجام می‌شود (پایین‌تر توضیح داده شده).
- دکمه «افزودن به ژست روز» به این نوار منتقل شد، کنار «تغییر عکس»، با آیکن `+` و متن
  «افزودن به پروژه روز».

**انتخاب ابعاد عکس هنگام آپلود:**
- با زدن «تغییر عکس»/«عکس مرجع»، اگر فایل گیف/وبق متحرک نباشد، به‌جای ذخیره مستقیم یک
  مودال جدید (`PhotoCropModal`) باز می‌شود: انتخاب قاب عمودی ۳:۴ یا افقی ۴:۳، جابه‌جایی
  عکس با کشیدن، زوم (با دکمه‌های +/− و بازنشانی)، و تأیید نهایی. عکس خروجی دقیقاً با همان
  نسبتی که انتخاب شده روی canvas برش می‌خورد و ذخیره می‌شود؛ نسبت انتخابی همان چیزی است
  که برای نمایش کارت بالای صفحه هم استفاده می‌شود (دیگر تاگل دستی لازم نیست).
- گیف/وبق متحرک همچنان مثل قبل بدون برش و بدون فشرده‌سازی مستقیم ذخیره می‌شود.

**گیف/عکس متحرک هم قابل جابه‌جایی و زوم شد:**
- چون گیف روی canvas فقط یک فریم ثابت می‌ماند و انیمیشن نابود می‌شود، برش پیکسلی
  واقعی برایش ممکن نیست؛ ولی حالا همان مودال برای گیف هم باز می‌شود، پیش‌نمایش زنده
  (خود گیف متحرک) نمایش داده می‌شود و کاربر می‌تواند جابه‌جا و زوم کند. فقط «موقعیت و
  زوم» ذخیره می‌شود (نه فایل جدید) و همه‌جا که آن ژست نمایش داده می‌شود همان قاب اعمال
  می‌گردد — خود گیف و انیمیشنش دست‌نخورده می‌ماند.
- این تنظیمات در `src/types/pose.ts` به‌صورت `photoCrop` روی ژست و در حافظه با
  `getPhotoCrops/setPhotoCrop/removePhotoCrop` نگه داشته می‌شود؛ در پشتیبان‌گیری/بازیابی
  (`buildBackup`/`restoreBackup`) هم لحاظ شده.

**دکمه‌های پایین:**
- دکمه آیکن‌دار «فیلم‌برداری همینه ژست» که کنار «بعدی» بود حذف شد (تکراری با دکمه
  «فیلم‌برداری این ژست» پایین‌تر بود).
- «فیلم‌برداری این ژست» بزرگ و تمام‌عرض شد (گرادیان رزگلد، برجسته‌تر).
- «بعدی» هم درست زیرش، تمام‌عرض و هم‌اندازه شد.
